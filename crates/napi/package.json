{
  "name": "finstream-napi",
  "version": "0.1.0",
  "description": "Node.js NAPI bindings for finstream",
  "main": "index.js",
  "scripts": {
    "build": "napi build --platform --release"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.6.2"
  }
}
